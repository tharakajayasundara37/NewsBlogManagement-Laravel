<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>@yield('title','NewsHub')</title><link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Playfair+Display:wght@700;800&display=swap" rel="stylesheet"><link rel="stylesheet" href="{{ asset('css/app.css') }}"></head>
<body><header class="top"><a class="brand" href="{{ route('home') }}"><span>NEWS</span>HUB</a><button class="menu" onclick="document.querySelector('.nav').classList.toggle('open')">☰</button><nav class="nav"><a href="{{ route('home') }}">Home</a><a href="{{ route('about') }}">About</a><a href="{{ route('contact') }}">Contact</a>@auth<a href="{{ route('dashboard') }}">Dashboard</a><form method="post" action="{{ route('logout') }}">@csrf<button>Logout</button></form>@else<a class="pill" href="{{ route('login') }}">Sign in</a>@endauth</nav></header>
@if(session('success'))<div class="flash">{{ session('success') }}</div>@endif @if($errors->any())<div class="flash error">{{ $errors->first() }}</div>@endif
<main>@yield('content')</main><footer><div><a class="brand" href="/">NEWSHUB</a><p>Independent stories, thoughtful perspectives and news that matters.</p></div><form method="post" action="{{ route('subscribe') }}">@csrf<label>Join our newsletter</label><div class="inline"><input type="email" name="email" placeholder="you@example.com" required><button>Subscribe</button></div></form></footer></body></html>
