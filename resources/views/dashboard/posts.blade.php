@extends('layouts.app') @section('title','Manage posts') @section('content')<section class="page"><h1>Posts</h1>@include('dashboard._nav')<table class="table"><tr><th>Title</th><th>Category</th><th>Status</th><th>Actions</th></tr>@foreach($posts as $post)<tr><td>{{ $post->title }}</td><td>{{ $post->category?->category_name }}</td><td>{{ $post->status }}</td><td><div class="actions"><a href="{{ route('dashboard.posts.edit',$post) }}">Edit</a><form method="post" action="{{ route('dashboard.posts.destroy',$post) }}">@csrf @method('delete')<button>Delete</button></form></div></td></tr>@endforeach</table>{{ $posts->links() }}</section>@endsection
