@extends('layouts.app') @section('title','Categories') @section('content')<section class="page"><h1>Categories</h1>@include('dashboard._nav')<div class="split"><form class="form-grid panel" method="post" action="{{ route('dashboard.categories.store') }}">@csrf<h2>New category</h2><input name="category_name" placeholder="Category name" required><textarea name="description" placeholder="Description"></textarea><button>Create</button></form><table class="table"><tr><th>Name</th><th>Slug</th><th></th></tr>@foreach($categories as $category)<tr><td>{{ $category->category_name }}</td><td>{{ $category->slug }}</td><td><form method="post" action="{{ route('dashboard.categories.destroy',$category) }}">@csrf @method('delete')<button>Delete</button></form></td></tr>@endforeach</table></div></section>@endsection
