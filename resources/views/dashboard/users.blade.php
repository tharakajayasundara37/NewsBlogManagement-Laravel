@extends('layouts.app') @section('title','Users') @section('content')<section class="page"><h1>Users</h1>@include('dashboard._nav')<div class="split"><form class="form-grid panel" method="post" action="{{ route('dashboard.users.store') }}">@csrf<h2>Add user</h2><input name="name" placeholder="Name" required><input type="email" name="email" placeholder="Email" required><input type="password" name="password" placeholder="Password (8+ characters)" required><select name="role"><option>author</option><option>admin</option></select><button>Create user</button></form><table class="table"><tr><th>Name</th><th>Email</th><th>Role</th></tr>@foreach($users as $user)<tr><td>{{ $user->name }}</td><td>{{ $user->email }}</td><td>{{ $user->role }}</td></tr>@endforeach</table></div></section>@endsection
